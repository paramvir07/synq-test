import { HeroHeader } from "@/components/header"
import HeroSection from "@/components/hero-section"

const page = () => {
  return (
    <div>
        <HeroHeader/>
        <HeroSection/>
    </div>
  )
}

export default page